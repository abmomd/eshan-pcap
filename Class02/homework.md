
---

## 🟢 Easy (1–5)

### Q1. Counter Increment
Create a class `Counter` with:
- An attribute `count` initialized to `0`
- A method `increment()` that increases `count` by 1  
Create an object and call `increment()` three times. Print the final value.

---

### Q2. Student Details
Create a class `Student` with attributes:
- `name`
- `age`  
Initialize them using a constructor and print the student details.

---

### Q3. Rectangle Area
Create a class `Rectangle` with attributes:
- `length`
- `breadth`  
Add a method `area()` that returns the area of the rectangle.

---

### Q4. Bank Balance
Create a class `BankAccount` with:
- Attribute `balance`
- Method `deposit(amount)`
- Method `withdraw(amount)`  
Create an object and perform one deposit and one withdrawal.

---

### Q5. Mobile Battery
Create a class `Phone` with:
- Attribute `battery = 100`
- Method `call()` that reduces battery by 10  
Call the method twice and print remaining battery.

---

## 🟡 Medium (6–10)

### Q6. Multiple Objects
Create a class `Car` with attribute `speed`.
Create **two objects** with different speeds and print their values.

---

### Q7. Object Interaction
Create two classes:
- `Engine` with method `start()`
- `Car` that contains an `Engine` object and calls `start()`  

---

### Q8. Equality vs Identity
Create two objects of a class `Point` with same `x` and `y` values.
Check:
- Equality using `==`
- Identity using `is`

---

### Q9. Update Attributes
Create a class `Employee` with attributes:
- `name`
- `salary`  
Add a method `increment_salary(percent)` to increase salary.

---

### Q10. Self Understanding
Write a class `Book` where:
- The constructor takes `title` and `price`
- A method `discount()` reduces price by 20%  
Explain using comments how `self` works.

---

## 🔴 Hard (11–15)

### Q11. Banking System (Improved)
Create a class `BankAccount` with:
- Private attribute `_balance`
- Methods `deposit()` and `withdraw()`
- Method `show_balance()`

---

### Q12. Counter with Limit
Create a class `LimitedCounter`:
- Starts from 0
- Has a max limit
- `increment()` should not exceed the limit

---

### Q13. Student–Course System
Create:
- `Course` class with `course_name`
- `Student` class that can enroll in a course  
Print enrolled course details.

---

### Q14. Identity Check
Create a class `Laptop`.
Create two references pointing to the **same object**.
Prove identity using `is`.

---

### Q15. Mini OOP Simulation
Design a small system using **at least 3 classes**  
Example ideas:
- School system
- Library system
- Online shopping cart  

Requirements:
- At least 3 attributes
- At least 3 methods
- Objects must interact with each other

---

## ✅ Instructions
- Use proper class naming conventions
- Use constructors wherever required
- Write clean and readable code
- Add comments where necessary

---


